package uk.co.brotherlogic.jarpur;

import junit.framework.TestCase;

public class SpeedTest extends TestCase
{

	public void testSpeed()
	{

		/*
		 * try { // Build the record page for the Girl Group Sound - Lost and
		 * Found /*long sTime = System.currentTimeMillis(); Properties lProps =
		 * new Properties(); lProps.load(new FileInputStream(new
		 * File("src/main/webapp/WEB-INF" + "/links.properties")));
		 * SimpleReplacer.setLinkTable(new LinkTable(lProps));
		 * 
		 * Page rPage = new RecordPage(); Map<String, String> objMap = new
		 * TreeMap<String, String>(); objMap.put("id", "6398");
		 * rPage.buildPage(objMap);
		 * 
		 * long eTime = System.currentTimeMillis() - sTime;
		 * 
		 * long dTime = Connect.getConnection().getTQueryTime();
		 * 
		 * Connect.getConnection().printStats();
		 * System.err.println("Database calls = " + dTime + " (" +
		 * Connect.getConnection().getSCount() + ")");
		 * System.err.println("Time to build page = " + (eTime - dTime)); }
		 * catch (IOException e) { e.printStackTrace(); assert (false); } catch
		 * (SQLException e) { e.printStackTrace(); }
		 * 
		 * assert (true);
		 */

	}
}
